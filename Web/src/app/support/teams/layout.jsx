export const metadata = {
  title: 'Teams Support - NovoTak',
  description: 'Complete guide to using the NovoTak Teams module - manage team members, roles, permissions, and organizational structure.',
};

export default function TeamsLayout({ children }) {
  return children;
}

