export const metadata = {
  title: 'Ticketing Support - NovoTak',
  description: 'Complete guide to using the NovoTak Ticketing module - manage tickets, track issues, and resolve customer support requests.',
};

export default function TicketingLayout({ children }) {
  return children;
}

